import { AuthController } from '../features/auth/auth.controller';
import { UserController } from '../features/user/user.controller';

export const ControllerModule = [AuthController, UserController];
